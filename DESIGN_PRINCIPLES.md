# 設計原則：エラー祭り対策

## 背景

実装中に以下の3つの典型的な問題が発生した：
1. ボタンが表示されているのにクリック不可（CSSのz-index問題）
2. 既存状態変数を使い回そうとして行ったり来たり
3. 不正な中間状態が生成され、直前にリセット処理を追加

これらは単なる実装ミスではなく、**プロンプト段階で状態空間と操作の制約を定義していなかった**ことが根本原因。

---

## 3つの設計公理

### 1. 到達可能性（Reachability）

```
仕様上可能な操作は、対応するすべての状態において物理的にも実行可能でなければならない。

Enabled(A, S) ⟹ Reachable(A, S)
```

**チェックリスト：**
- [ ] 表示されているUI要素はすべてクリック可能か
- [ ] overlay、z-index、pointer-events で隠れていないか
- [ ] scroll領域の問題はないか
- [ ] disabled状態が誤設定されていないか

**過去の問題：** ボタンがz-indexで隠れていた

---

### 2. 状態十分性（State Sufficiency）

```
意味的に独立した情報は、独立した状態変数として表現する。
既存状態から推定して代用してはならない。

異なる意味状態は異なる内部状態で表現する。
```

**チェックリスト：**
- [ ] 新機能に必要な情報は既存stateだけで一意に表現できるか
- [ ] 複数の意味が同じ変数に混在していないか
- [ ] 既存変数を「使い回し」ていないか
- [ ] 新しい状態変数追加は許可されているか

**過去の問題：** loginView/homeView の状態が1変数では不足していた

---

### 3. 不正状態禁止（State Closure）

```
仕様に存在しない中間状態を作らない。
不正状態を生成した後にreset・cleanup・補正処理で正常状態へ戻す実装は解決策ではない。

可能なら不正状態そのものを表現不能にする。

ValidState → ValidState のみが許可される遷移。
```

**チェックリスト：**
- [ ] 発生してはいけない状態が表現されていないか
- [ ] cleanup/reset処理が追加されていないか（症状の治療）
- [ ] 根本的に状態遷移を排除しているか（原因の治療）
- [ ] すべての操作が必ず有効状態に遷移するか

**過去の問題：** ログイン画面の自動非表示で不正な中間状態が生成された

---

## 実装前チェックリスト

新機能追加前に必ず以下を確認：

- [ ] 現在存在する意味状態を全列挙
- [ ] 今回追加される意味状態を定義
- [ ] 各状態から許可される操作を定義
- [ ] 各操作による状態遷移を図化
- [ ] 発生してはいけない状態を明記
- [ ] 新しい独立状態変数が必要か判定
- [ ] 今回の変更で既存状態変数の意味が変化しないか確認

その後に実装を開始。

---

## デバッグ停止条件

**同じ箇所を2回以上修正したら停止し、状態モデルを検査する。**

例：
- 「この値をtrueにするとAが動くがBが壊れる」
- 「直前に状態を消す」「直後に値を戻す」
- 「特定ケースだけ例外処理する」

↑ これらは**構造的欠陥の兆候**。

**patch mode ⟹ model inspection mode に切り替える。**

---

## 参考：状態設計の順序

```
State Space（何が状態か？）
    ↓
Transition Rules（どう遷移するか？）
    ↓
Operation Algebra（どう操作するか？）
```

下から上へ設計してはならない。上から下へ。

---

## 用語

- **Reachable(A, S)**: 状態Sで操作Aが物理的に実行可能
- **Enabled(A, S)**: 状態Sで操作Aが仕様上許可されている
- **ValidState**: 仕様に存在する正当な状態
- **IllegalState**: 仕様に存在しない不正な状態
- **State Closure**: すべての遷移がValidStateを保つ性質
